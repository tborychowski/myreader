@import "_lib";

.sidebar
	pos 0 auto 0 0; width sidebar-w
	padding-top 50


.sidebar-logo
	pos 0; left 0; font 24; color #999; cursor default; opacity .3;
	padding 5 0 0 10; not-selectable();

	i
		font 34; margin-right -3; vertical-align bottom; show 1;
		&:before
			rotate 270; show 2;


.source-tree
	pos 120 0 0 0; padding 0 10 10;
	.source-folder
		b 0; padding 0; margin 30 0 10;
		&:first-of-type
			margin-top 0;

.source-list
	list-style none; margin 0; padding 0


.source-folder
.source-item
	// show 0;

	&.visible
		// show 2;
		color #fff;

